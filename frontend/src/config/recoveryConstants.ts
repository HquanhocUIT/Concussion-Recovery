/**
 * Product mapping used for the current MVP.
 *
 * UI screen_time is a 0–5 exposure score, not literal minutes.
 */
export const SCREEN_SCORE_TO_MINUTES = 30;

export const DEFAULT_LIGHT_EXERCISE_MINUTES = 30;

export const DEFAULT_REST_MINUTES = 60;